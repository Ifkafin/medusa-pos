# Required by build:fedora so the installed POS starts against the intended
# Medusa deployment. This is an origin, not a secret; never embed credentials.
VITE_BACKEND_URL="https://medusa-staging.example.com/"
